name: CI

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Used golang 1.15
      uses: actions/setup-go@v2
      with:
        go-version: 1.15
    - run: go mod download
    - run: make check
    - run: make tests
